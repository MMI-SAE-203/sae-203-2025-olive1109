import { pb, getFilms, getFilmId, getAteliers, Invitealphabet, ActiviteId, getInviteId } from '../path_to_your_backend_file';


/*
try {
    const films = await getFilms();
    console.table(films);
} catch (e) {
    console.error(e);
}

try {
    const film = await getFilmId('1');
    console.table(film);
} catch (e) {
    console.error(e);
}

try {
    const ateliers = await getAteliers();
    console.table(ateliers);
} catch (e) {
    console.error(e);
}

try {
    const invites = await Invitealphabet();
    console.table(invites);
} catch (e) {
    console.error(e);
}

try {
    const activite = await ActiviteId('10');
    console.table(activite);
} catch (e) {
    console.error(e);
}

try {
    const invite = await getInviteId('5');
    console.table(invite);
} catch (e) {
    console.error(e);
}
*/
